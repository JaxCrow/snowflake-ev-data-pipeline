name: 'ev_pipeline'
version: '1.0.0'
config-version: 2
profile: 'ev_pipeline'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  ev_pipeline:
    staging:
      +materialized: view
    gold:
      +materialized: table
